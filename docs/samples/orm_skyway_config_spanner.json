{
    "_comments": [
        "Sample configuration for Google Spanner with the PostgreSQL interface (EXPERIMENTAL - not yet verified end-to-end).",
        "",
        "Two connection options are provided below. Option B (PGAdapter + pgjdbc) is recommended",
        "as it uses the same PostgreSQL JDBC driver as all other PostgreSQL-family databases,",
        "and is fully compatible with orm_skyway's UUID support.",
        "",
        "=== OPTION B (Recommended): PGAdapter + standard PostgreSQL JDBC driver ===",
        "",
        "Step 1: Start the combined PGAdapter + Spanner emulator Docker container (Windows):",
        "   docker run -d -p 5432:5432 -p 9010:9010 -p 9020:9020 gcr.io/cloud-spanner-pg-adapter/pgadapter-emulator",
        "",
        "   This starts both PGAdapter and the Spanner emulator in one container. No Google Cloud",
        "   credentials or project needed. Any database name you connect to is auto-created.",
        "   Data is stored in memory only -- all state is lost when the container stops.",
        "",
        "Step 2: Use this config with Option B settings below.",
        "   jdbc_url points to PGAdapter on localhost:5432 using standard PostgreSQL JDBC.",
        "   jdbc_driver_jar points to the standard PostgreSQL JDBC driver (already bundled in Docker mode).",
        "",
        "=== OPTION A (Fallback): Spanner JDBC driver directly ===",
        "",
        "   Uncomment the Option A settings and comment out Option B.",
        "   Download the Spanner JDBC driver from https://cloud.google.com/spanner/docs/jdbc-drivers",
        "",
        "Copy to your project root, rename to orm_skyway_config.json, and edit for your database.",
        "Run: python orm_skyway.py -f orm_skyway_config.json --phase 1+3"
    ],

    "_option_b_recommended": "PGAdapter + standard PostgreSQL JDBC driver",
    "jdbc_url":          "jdbc:postgresql://localhost:5432/test-db",
    "db_schema":         "",
    "db_user":           "",
    "db_password":       "",
    "jdbc_driver_jar":   "C:/SoftwareTree/JDX5.x/external_libs/postgresql-42.2.29.jar",
    "jdbc_driver_class": "org.postgresql.Driver",

    "_option_a_fallback": "Spanner JDBC driver directly (uncomment and comment out Option B above)",
    "_option_a_jdbc_url": "jdbc:cloudspanner://localhost:9010/projects/emulator-project/instances/test-instance/databases/test-db;usePlainText=true",
    "_option_a_jdbc_driver_jar": "C:/drivers/google-cloud-spanner-jdbc-x.x.x.jar",
    "_option_a_jdbc_driver_class": "com.google.cloud.spanner.jdbc.JdbcDriver",

    "jx_home":           "C:/SoftwareTree/JDX5.x",
    "object_model_package": "com.example.json.model",
    "reverse_eng_template_config": "reverse_eng_template",
    "tables":            "",
    "model_overview":    "A Google Spanner database object model",

    "docker_image_name": "my-spanner-gilhari-service",
    "docker_image_tag":  "1.0",
    "gilhari_host_port": 80,

    "skip_reverse_eng":  false,
    "skip_compile":      false,
    "verbose":           false
}
